package ce1002.s103403513;

import javafx.application.Application;
import javafx.event.ActionEvent;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.image.Image;
import javafx.scene.layout.Background;
import javafx.scene.layout.BackgroundImage;
import javafx.scene.layout.BackgroundPosition;
import javafx.scene.layout.BackgroundRepeat;
import javafx.scene.layout.BackgroundSize;
import javafx.scene.layout.Region;
import javafx.stage.Stage;

public class Main extends Application {
	Scene menu,gameview;
    @Override
    public void start(Stage primaryStage) throws Exception{
    	
    	FXMLLoader fxmlLoader = new FXMLLoader(getClass().getResource("/gameview.fxml"));
        Controller controller = new Controller();
        fxmlLoader.setController(controller);
        Parent root = fxmlLoader.load();
        /*
        BackgroundImage myBI= new BackgroundImage(new Image("src/image/background.jpg"),
                BackgroundRepeat.REPEAT, BackgroundRepeat.NO_REPEAT, BackgroundPosition.DEFAULT, BackgroundSize.DEFAULT);
        ((Region) root).setBackground(new Background(myBI));
        */
        gameview = new Scene ( root, 1200, 800 );
        
        //gameview.getStylesheets().addAll(this.getClass().getResource("gameview.css").toExternalForm());
        primaryStage.setTitle("Game");
        
        controller.setScene (gameview);
        primaryStage.setScene(gameview);
        primaryStage.show();
        /*

        fxmlLoader = new FXMLLoader(getClass().getResource("/menu.fxml"));
        MenuController Menucontroller = new MenuController();
        fxmlLoader.setController(Menucontroller);
        root = fxmlLoader.load();
        menu = new Scene ( root, 1200, 800 );
        Menucontroller.setScene (menu);
        primaryStage.setScene(menu);
        primaryStage.show();
        */
    }


	public static void main(String[] args) {
        launch(args);
    }
}
